# 第十二章 Agent协作模块：专家Agent + 协调Orchestrator Agent架构
## 12.1 模块概述
本模块将原“AI认知搭档”拆解为**垂直领域专家Agent集群 + 协调Agent(Orchestrator)**。
- 垂直专家Agent：每个Agent专精单一领域，输出本领域事实、指标、局部判断，并可输出**买卖/仓位方向建议（建议级，含置信度/纪律依据/触发条件）**；**禁止自动执行、禁止绕过人下单**。
- 协调Agent(Orchestrator)：唯一人类交互入口；负责任务拆解、分发专家Agent、结果汇总、总纲规则校验、冲突识别消解、输出综合报告。
- 交互范式：**人类只和协调Agent对话，不直接调用任意垂直专家Agent**；专家Agent之间不直接通信，全部经由协调Agent中转调度。

> 核心定位：Agent集群仅作为分析辅助工具，**不替代人类做最终交易决策**；人类保留底层信念定义、风控底线、最终交易决策、体系迭代、冲突最终裁决的全部权责。

## 12.2 模块顶层硬约束（不可绕过）
1. **体系总纲最高优先级**：所有Agent输出、调度逻辑必须对齐《个人投资体系总纲》底层信念、风控红线、能力圈、选股一票否决清单。专家Agent输出如果违反总纲规则，协调Agent必须拦截、标记风险、降低权重或直接舍弃该结论。
2. **人类唯一入口原则**：人类只向协调Agent下发任务、接收综合报告；不直接调用垂直专家Agent；专家Agent原始输出仅用于溯源查阅。
3. **分工隔离原则**：垂直专家Agent输出本领域事实、指标、局部判断，可输出**买卖/仓位方向建议（建议级，禁自动执行）**；交易、仓位、策略适配的最终综合建议由协调Agent结合总纲规则汇总生成；**最终决策权始终在人**。
4. **冲突显式化原则**：多专家输出出现矛盾，不静默合并；必须显式列出冲突点、各方论据、冲突原因；能依据总纲自动消解则消解，无法消解则标记交由人类裁决。
5. **可溯源原则**：每条结论标注来源Agent、原始依据、置信度；支持调取各专家Agent原始输出。
6. **弱预测原则**：禁止确定性行情预测；所有结论附带置信度、适用边界、局限性。

## 12.3 整体架构
人类用户 ↔ 【协调 Agent (Orchestrator)】
↓↑任务调度
┌────────┬────────┬────────┬────────┬────────┬────────┬────────┐
财报解读 Agent｜宏观周期 Agent｜行业景气 Agent｜市场情绪 Agent｜技术形态 Agent｜标的合规风控 Agent｜大类资产配置辅助 Agent
（垂直专家 Agent 集群，对人类不可直接访问）

## 12.4 垂直专家Agent清单与职责边界
> 每个专家Agent输出结构固定：【观测事实】【指标集合】【本领域局部判断】【方向建议（建议级）】【置信度0‑100】【纪律依据】【触发条件】【局限性】【风险提示】；**方向建议仅供人类决策参考，禁止自动执行**。

### 12.4.1 财报解读Agent
**专精领域**：企业财报、财务质量、盈利、现金流、ROE、商誉、负债、扣非、分红；执行总纲第八章选股量化指标校验。
**能力**
1. 计算价值/GARP/逆向选股全部量化指标；
2. 识别财务异常，匹配总纲8.4通用一票否决清单财务条件；
3. 评估盈利质量、现金流质量、分红持续性；
4. 区分一次性损益与主营业务拐点；
5. 输出财务健康度评分、指标偏离预警、财务风险清单。
**边界约束**：不做行业景气判断、不做估值外推定价，只输出财务事实与财务风险。

### 12.4.2 宏观周期Agent
**专精领域**：宏观指标采集、四周期识别，对应总纲第十章宏观周期判断框架。
**能力**
1. 采集PMI、社融、CPI、PPI、政策利率等总纲定义观测指标；
2. 按总纲规则识别周期阶段，输出指标状态、信号一致性、信号矛盾点；
3. 输出宏观简报，标记周期切换初步信号；
4. 严格遵守模糊正确、交叉验证、低频调整，拒绝精准拐点预测。
**边界约束**：不输出个股买卖，不输出最终大类配置比例；仅输出宏观状态事实。

### 12.4.3 行业景气Agent
**专精领域**：赛道景气、渗透率、供需格局、产业政策、能力圈校验；对应总纲第六章能力圈与标的池。
**能力**
1. 跟踪行业景气指标：渗透率、供需、政策、市场份额变化；
2. 识别景气上行/见顶/下行信号；
3. 校验标的是否落在一/二/三级能力圈，识别越界标的；
4. 区分短期事件扰动和中长期产业逻辑变化。
**边界约束**：不做个股估值定价，仅输出行业事实与景气判断。

### 12.4.4 市场情绪Agent
**专精领域**：市场情绪、板块情绪、标的估值分位、回撤、错杀识别；服务逆向投资策略。
**能力**
1. 观测标的/板块回撤幅度、估值历史分位、市场一致预期；
2. 辅助识别逆向场景：短期事件利空、情绪错杀；
3. 区分“情绪性下跌”与“基本面不可逆恶化”；
4. 输出情绪极值信号、共识偏差。
**边界约束**：情绪信号不能单独作为抄底依据，仅作为证据维度。

### 12.4.5 技术形态Agent（轻量化趋势Agent）
**专精领域**：价格趋势、动量、破位、支撑压力；对应总纲4.4轻量化趋势策略。
**能力**
1. 识别单边趋势、震荡市、趋势破位、假突破；
2. 输出趋势信号；严格执行总纲约束：震荡市自动停用趋势做多信号。
**边界约束**：纯技术信号不具备决策效力；必须绑定基本面合格前提；不独立生成交易信号。

### 12.4.6 标的合规风控Agent
**专精领域**：标的合规、风险拦截、一票否决校验，对应总纲8.4一票否决清单、6.3投资禁区。
**能力**
1. 校验标的是否命中通用一票否决清单；
2. 监控实控人变动、监管处罚、退市风险、重大舆情风险；
3. 校验是否属于永久投资禁区；
4. 输出阻断结论：标记“禁止进入持仓/跟踪池”。
**边界约束**：本Agent否决结论具备高优先级，协调Agent必须优先采纳。

### 12.4.7 大类资产配置辅助Agent
**专精领域**：大类资产性价比、估值分位、股息、黄金、债券、现金比价；对应总纲7.1大类资产配置区间。
**能力**
1. 输出股票、债券、黄金、红利资产估值分位、性价比；
2. 对照总纲周期‑配置表格输出参考配比提示；
3. 提示调仓约束：单次调仓不超过总仓位15%、权益最低仓位≥30%。
**边界约束**：不输出最终调仓指令，仅输出参考区间。

## 12.5 协调Agent（Orchestrator）工作流程
1. **任务解析**：接收人类请求，识别任务类型：标的深度评估、大类资产季度评估、持仓组合风险扫描、周度/季度复盘辅助、交易计划合规校验。
2. **子任务分发**：根据任务类型，选择需要调用的垂直专家Agent，下发子任务；不调用无关Agent避免冗余。
3. **结果收集 & 总纲硬规则校验**
    - 优先执行一票否决、能力圈、仓位、风控、策略边界校验；
    - 如果合规风控Agent输出禁止标的，直接终止分析，输出拦截理由。
4. **冲突识别**：遍历各Agent输出，识别观点冲突；记录冲突点、各方论据、冲突原因。
5. **冲突消解**
    - 优先以《个人投资体系总纲》作为裁判依据；
    - 财务、合规类负面结论优先级高于情绪、技术信号；
    - 无法自动消解的冲突，不强行合并，原样列出，交由人类裁决。
6. **生成综合报告**
    报告固定结构：
    1）任务概述
    2）总纲规则校验结果（一票否决、能力圈校验）
    3）多维度专家摘要（各Agent方向建议简写）
    4）冲突与不确定性说明（如有）
    5）基于投资体系的综合评估：机会点、风险点
    6）综合建议：方向 + 参考仓位区间 + 触发条件 + 置信度（建议级，禁自动执行）
    7）体系适配策略建议（适合价值/GARP/逆向/不适合/禁止参与）
    8）参考仓位上限（严格取自总纲仓位规则，不主观创造）
    9）溯源索引：可调取各Agent原始输出
> 重要约束：协调Agent可输出**买卖/仓位方向的综合建议（建议级，含置信度/触发条件/纪律依据）**，但**禁止自动执行、禁止绕过人下单**。最终交易决策留给人类。

## 12.6 协调Agent内置不可关闭硬约束
1. 任何专家Agent输出违背体系总纲底层信念、风控红线，必须标记风险，降低权重或直接丢弃；
2. 技术形态Agent信号不能脱离基本面单独生效；
3. 逆向机会必须同时具备财报Agent+情绪Agent双重正向证据；
4. 输出必须附带置信度与局限性，禁止绝对化预测语言；
5. 仓位、配比建议严格引用总纲表格，不得自行发明仓位数值。

## 12.7 典型任务场景
1. **标的入池评估**：调用财报解读Agent、标的合规风控Agent、行业景气Agent、市场情绪Agent、技术形态Agent；做入池合规校验，输出是否适合跟踪池。
2. **季度大类配置简报**：调用宏观周期Agent、大类资产配置辅助Agent；输出周期判断、参考配置区间、不确定性。
3. **交易计划校验**：接收人类提供的交易计划（标的、计划仓位、策略类型、买卖理由、止盈止损），调用对应Agent，对照总纲输出合规点、违规点、风险提示。

## 12.8 人机交互规则
1. 人类输入：自然语言任务，可附带标的代码、交易计划、周期指令。
2. 人类接收：协调Agent输出统一综合报告。
3. 人类可执行操作：
    - 下达新分析任务；
    - 对冲突点人工裁决；
    - 请求调取某Agent原始输出（仅溯源，不直接调用Agent干活）；
    - 将报告作为参考，输出个人交易决策、复盘记录回写体系文档。

## 12.9 附录：模块风险与局限性
1. Agent集群仅为分析辅助；数据存在滞后、错误可能，不能替代人类逻辑校验。
2. Agent只能执行已有总纲规则，无法识别文档未覆盖的新型风险；体系迭代必须由人类主导。
3. 当宏观信号矛盾、处于周期过渡期，Agent会输出不确定性，需要人类主观裁决。
4. Agent不做自动实盘交易，不会自动下单，全部输出仅为建议级方向与分析报告，不构成执行指令。

## 12.10 后续可选扩展（暂不实现）
1. Agent记忆库：保存历史标的分析、周期判断，用于复盘回溯。
2. Agent输出质量自检：比对Agent历史结论与后续事实，识别Agent自身偏差。
3. 批量扫描一级/二级标的池，输出全池风险简报。
