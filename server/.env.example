# 认知投资工作台 · 后端环境变量
# 部署到 Railway 时，请在 Railway 项目的 Variables 面板中填写以下变量（不要提交真实密钥到仓库）。
# 本地调试：复制本文件为 server/.env 后填写。

# ===== 必填 =====

# DeepSeek API Key（对话大脑）。获取：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-请填写你的DeepSeekKey

# GitHub Token（数据层：读写私有仓库 investment-data / investment-system）。
# 需具备 repo 权限（读写私有仓库）。获取：https://github.com/settings/tokens
# 注意：Railway 上必须显式填写此变量，因为服务端没有 gh CLI 登录态。
GITHUB_TOKEN=请填写具备repo权限的GitHubToken

# ===== 可选 =====

# 服务端口（Railway 会自动注入 PORT，本地默认 8787）
PORT=8787

# 运行环境（Railway 部署建议 production；本地可不填）
NODE_ENV=production
